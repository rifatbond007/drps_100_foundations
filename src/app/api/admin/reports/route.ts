/**
 * GET /api/admin/reports — donation reports (overview, by date range, CSV export)
 *
 * SKELETON.
 */
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ error: 'NOT_IMPLEMENTED' }, { status: 501 });
}
