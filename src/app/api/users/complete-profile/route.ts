/**
 * POST /api/users/complete-profile
 *
 * First-time user setup: phone + language preference.
 *
 * SKELETON.
 */
import { NextResponse } from 'next/server';

export async function POST() {
  return NextResponse.json({ error: 'NOT_IMPLEMENTED' }, { status: 501 });
}
